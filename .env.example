# Postgres — Railway auto-injects this when you attach a Postgres add-on.
# For local dev, point at a local Postgres or your Railway DB's public URL.
DATABASE_URL=postgres://user:password@host:5432/dbname

# Anthropic API key — set in Railway dashboard, never commit.
ANTHROPIC_API_KEY=sk-ant-...
